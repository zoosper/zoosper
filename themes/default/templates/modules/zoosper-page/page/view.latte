{* Theme override for zoosper-page::page/view. Page body HTML is sanitised before persistence. *}
<article class="page page-{$page->slug}">
    <header class="page-header">
        <h1>{$page->title}</h1>
    </header>
    <div class="page-content">{$page->content|noescape}</div>
</article>
