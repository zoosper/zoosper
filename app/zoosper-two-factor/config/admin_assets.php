<?php

declare(strict_types=1);

return [
    'assets' => [
        /*
         * Reserved for the future admin 2FA setup/challenge screens.
         *
         * Do not inject OTP values, TOTP secrets, recovery codes, provisioning
         * URIs or QR data into asset declarations. Those values are sensitive
         * runtime data and must never be exposed through static asset config.
         */
    ],
];
