# Zoosper Menu

Site-scoped navigation foundation for Zoosper 0.2 Alpha. The module owns menu persistence, nested item validation, deterministic tree building, breadcrumbs and the public `/api/v1/menu` representation. Admin CRUD is intentionally implemented in the next adoption layer on top of these contracts, never in the domain services.
