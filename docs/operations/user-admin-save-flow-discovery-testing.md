# UserAdminController save-flow discovery testing

Run:

```bash
php -l tools/run-verification-suite.php
php tools/run-verification-suite.php --output=var/reports/latest-verification.txt
```

Inspection output is also written to:

```text
var/reports/user-admin-save-flow-inspection.txt
```

Expected:

```text
Overall result: OK
```
