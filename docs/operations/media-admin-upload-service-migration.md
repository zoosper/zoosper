# Media admin upload service migration operations

The package-local migration instructions live in:

```text
packages/zoosper-media/docs/operations/media-admin-upload-service-migration.md
```

Root operations docs keep this link so the future documentation website can index the package-owned guide without duplicating implementation detail.
