# Frontend sanitised HTML rendering documentation seed

Future docs should explain:

```text
Why stored CMS HTML is sanitised before persistence
Why frontend templates render content with noescape
How this changes once block_json rendering becomes active
How to avoid double escaping
```
