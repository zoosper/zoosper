# Editor.js tools documentation seed

Future docs should explain:

```text
How heading/list tools are registered
Why h1 is not available inside page body
How the temporary HTML bridge works
Why server-side sanitisation still applies
When block_json storage will replace the bridge
```
