# UserAdminController rendering pattern review documentation seed

Future documentation should explain that controller-rendered forms need safe PHP-variable interpolation, not raw PHP template tags inside source strings.
