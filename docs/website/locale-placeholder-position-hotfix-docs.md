# Locale placeholder position hotfix docs seed

Future documentation should explain that heredoc UI patching must insert placeholders between complete form controls, not into partially interpolated input attributes.
