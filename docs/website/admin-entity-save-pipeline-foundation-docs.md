# Admin entity save pipeline foundation docs seed

Future documentation should explain how module authors can contribute fields, how data enters `EntityDataObject`, how core write maps protect SQL writes, and how extension data is persisted separately.
