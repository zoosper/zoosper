# Admin user locale persistence documentation seed

Future documentation should explain how `admin_users.locale` is saved, how blank values fall back to configured admin locale, and how the admin-context translator consumes the locale after the next request.
