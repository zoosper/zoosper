# Admin message documentation seed

Future docs should explain:

```text
How to add a flash message from a controller
Message types and keys
Why keys prevent duplicate piling
How AJAX saves will return messages later
Security rule: messages must never include secrets or raw exception traces
```
