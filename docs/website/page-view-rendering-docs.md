# Page view rendering documentation seed

Future docs should explain:

```text
Why page body HTML is rendered without escaping after sanitisation
Why titles/slugs remain escaped
How theme module overrides are selected before module resource views
How to avoid double escaping in custom themes
```
