# Locale UI login controller hotfix documentation seed

Future documentation should explain that admin preference UI must be integrated into explicit user/profile forms rather than broad text-matched controllers such as the login controller.
