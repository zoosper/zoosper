# PDO locale parameter and error notice hotfix docs seed

Future documentation should explain that SQL token/parameter parity is required for all repository write methods and that admin notices use `.notice-success`, `.notice-error` and future `.notice-warning` classes.
