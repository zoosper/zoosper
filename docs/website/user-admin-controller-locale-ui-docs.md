# Explicit UserAdminController locale UI documentation seed

Future documentation should explain where the admin-user locale field appears, why LoginController must not be patched, and how the field will later be persisted.
