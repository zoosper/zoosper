# Extension data persistence planning docs seed

Future developer documentation should explain:

- How modules contribute field definitions.
- How submitted form values enter `EntityDataObject`.
- How core write maps protect core SQL.
- How `ExtensionTable` fields persist separately.
- When module authors should use module-owned tables instead of the generic extension-value table.
