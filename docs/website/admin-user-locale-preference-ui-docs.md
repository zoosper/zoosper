# Admin user locale preference UI documentation seed

Future documentation should explain how the locale preference field is rendered from SupportedLocaleProvider and how blank locale values fall back to the configured admin locale.
