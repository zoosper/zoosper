# Admin translator runtime wiring documentation seed

Future docs should explain:

```text
How PageAdminController fallback translation is wired
Why AdminTranslatorResolver owns locale + translator orchestration
How a future DI/container binding can replace the controller fallback
How to run the verification suite and attach latest-verification.txt
```
