# UserAdminController pipeline locale persistence docs seed

Future documentation should explain how the admin user form normalises locale through the save pipeline and persists it through repository create/update methods.
