# UserAdminController locale UI verifier scope hotfix docs seed

Future documentation should explain that verifier source scans must target the specific injected/rendered block, not the whole PHP file when the token is expected in normal PHP source.
