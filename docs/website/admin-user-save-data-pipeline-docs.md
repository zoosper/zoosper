# AdminUser save data pipeline documentation seed

Future documentation should explain how submitted AdminUser form values become `EntityDataObject`, how locale is normalised, and how core writes are created only from field definitions.
