# Admin user locale hydration hotfix docs seed

Future documentation should explain why locale hydration tools must target the concrete `AdminUser` model and avoid matching helper/resolver classes that share the same prefix.
