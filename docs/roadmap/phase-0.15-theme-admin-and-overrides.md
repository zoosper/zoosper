# Phase 0.15 - Theme Admin and Template Overrides

Recommended next theme phase:

- add `/admin/sites` or extend existing site admin
- allow selecting `theme_code` from installed themes
- list installed themes from `themes/*/theme.php`
- support module template overrides
- add basic layout variants
- add cache key strategy for theme templates
