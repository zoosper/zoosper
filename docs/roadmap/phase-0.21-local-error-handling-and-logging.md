# Phase 0.21 - Local Error Handling and Module Logging

Recommended next phase:

- wire `LogManager` into `ApplicationFactory`
- register a local exception/error handler for development logs
- add module loggers to controller providers
- allow module-specific log filenames from config
- add admin screen for recent local logs later
- avoid logging secrets, passwords, session IDs or tokens
