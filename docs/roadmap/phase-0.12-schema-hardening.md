# Phase 0.12 - Declarative Schema Hardening

Next improvements after the MVP declarative schema engine:

- schema snapshot persistence
- destructive operation allow-list
- rename mappings
- foreign key support
- schema validation command
- generate db_schema.php from live database
- integrate schema apply into `php bin/zoosper migrate`
