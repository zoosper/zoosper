# Phase 0.19 - Pages, Users and Roles View Refactor

Recommended next phase:

- move `PageAdminController` HTML to module views
- move `UserAdminController` HTML to module views
- move `RoleAdminController` HTML to module views
- introduce reusable admin form components
- introduce reusable admin permission tree component
- introduce reusable admin user assignment component
