# Phase 0.7 - Dynamic Module Routes Roadmap

Next extension step:

- add `config/admin_routes.php` support per module
- add `config/api_routes.php` support per module
- add a route loader to wire module routes into the router
- add validation so missing controller classes fail loudly
- add docs for marketplace module structure
- add example module under `modules/zoosper-example-admin`
