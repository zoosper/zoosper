# Phase 0.17 - Admin Components and Module UI

Recommended next phase:

- move admin forms/tables/cards into reusable template partials
- introduce admin UI components
- allow modules to provide admin views under `resources/views/admin`
- add theme overrides for admin module views
- reduce inline HTML inside admin controllers
