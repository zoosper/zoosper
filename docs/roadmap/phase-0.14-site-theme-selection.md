# Phase 0.14 - Site Theme Selection and Layouts

Recommended next frontend/theme phase:

- add `theme_code` to sites or site settings
- make `SiteResolver` expose current theme
- support `layout.php` and content templates
- add template partials for header/footer
- render menus from a future `zoosper-menu` module
- prepare for module template overrides
```
