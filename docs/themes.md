# Themes

Themes provide frontend templates and assets and are assigned per site. The default theme demonstrates site layout and Page rendering.

Latte is the current default template engine. Zoosper remains API-first and should allow alternative template engines through replaceable integration contracts.

Theme templates should consume prepared view data rather than query repositories directly.
