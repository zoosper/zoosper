# Grid command-bar popup alignment

The saved-view management panel is positioned from the Manage saved views trigger rather than from the page edge. Desktop width is bounded to 680 pixels and the panel is kept inside the viewport. The panel includes an explicit close button, supports Escape and outside-click closure, and restores focus to the trigger after keyboard or close-button dismissal. Narrow screens continue to use a bottom drawer.
