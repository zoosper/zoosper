# Saved Grid view selector

The compact Grid toolbar now renders the saved bookmark collection as a selector. Selecting a bookmark navigates to the existing `bookmark_id` query contract, so the server remains authoritative for resolving filters, sorting, page size, visible columns and column order. The selector marks the active bookmark and labels the default bookmark. The redundant top Save view button was removed; creation and mutation remain in Saved Grid settings.
