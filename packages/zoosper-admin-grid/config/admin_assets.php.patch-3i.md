# Phase 3I Admin Grid asset addition

Merge this stylesheet into the existing `stylesheets` list:

```php
[
    'path' => 'resources/admin/css/grid-workspace-status.css',
    'priority' => 71,
],
```
