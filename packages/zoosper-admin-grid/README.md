# zoosper/admin-grid

Reusable Admin-grid workspace presentation and per-Admin state, including selected columns, ordering, bookmarks, page size, protected mutations and audited CSV export integration.

This package does not own feature repositories, authentication or host routes. See the canonical [Admin extension guide](../../docs/admin.md).
