# Zoosper Errors

Exception presentation, sensitive-value redaction and CLI formatting for Zoosper. `ZoosperException` extends the Marko exception contract while adding documentation links and structured redacted details.

The package is registered as a Composer `zoosper-module` with `extra.marko.module` set to `true`. See the canonical [module guide](../../docs/modules.md).
