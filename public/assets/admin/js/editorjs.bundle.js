(function () {
  'use strict';
  // Placeholder bundle for Phase 0.69 so the admin page does not 404 before npm build.
  // Run `npm install` and `npm run build:admin-editor` to replace this with the local Editor.js bundle.
  window.ZoosperEditorJsBundle = window.ZoosperEditorJsBundle || { placeholder: true, version: 'phase-0.69-placeholder' };
}());
